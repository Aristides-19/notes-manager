package com.cavenaire.notesmanager.view.observer;

/**
 * If a container like {@code dashboard} contains {@code observables} components, it must have implementation
 * in this package where each {@code observable} is injected in the {@code observable container},
 * and each method updates each {@code observable}.
 */
public interface ObservableContainer {
}
